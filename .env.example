# Signing key for JWT access tokens.
# Min 32 chars. Generate locally with: openssl rand -hex 32
# Set identically on Vercel (Production + Preview + Development).
JWT_SECRET=replace-with-32-plus-char-random-string
